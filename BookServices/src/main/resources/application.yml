server:
  port: 8080
spring:
  application:
    name: Book-Service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/bookstoredb
    username: root
  jpa:
    hibernate:
      ddl-auto: 'update'
    show-sql: 'true'
